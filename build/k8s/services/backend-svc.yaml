apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: dddance-dev
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 5458
      targetPort: 5458
      protocol: TCP
