apiVersion: v1
kind: Service
metadata:
  name: ml-service
  namespace: dddance-dev
  labels:
    app: ml-service
spec:
  type: ClusterIP
  selector:
    app: ml-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
