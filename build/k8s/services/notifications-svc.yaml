apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: dddance-dev
  labels:
    app: notifications
spec:
  type: ClusterIP
  selector:
    app: notifications
  ports:
    - name: grpc
      port: 5460
      targetPort: 5460
      protocol: TCP
